# C-HTTP

**HTTP/1.1** server written in C. Supported methods:
- GET
- HEAD
- OPTIONS

The default server port is 27015.

To compile the server:
```
make server
```
Then run:
```
./server.exe
```

To compile the client:
```
make client
```

Then run:
```
./client.exe localhost
```
